/** Returns the current UTC calendar month as `YYYY-MM`. */
export function getCurrentPeriodKey(date: Date = new Date()): string {
  const year = date.getUTCFullYear();
  const month = String(date.getUTCMonth() + 1).padStart(2, "0");
  return `${year}-${month}`;
}
